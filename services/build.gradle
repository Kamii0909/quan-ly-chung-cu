plugins {
    id 'java-library'
    id 'eclipse'
}

repositories {
    mavenCentral()
}

dependencies {
    api(libs.guava)
    api(libs.money)
}

tasks.named('test') {
    useJUnitPlatform()
}

eclipse {
    classpath {
        baseSourceOutputDir = file('build/eclipse')
    }
}