plugins {
    id 'java-library'
    id 'eclipse'
}

repositories {
    mavenCentral()
}

dependencies {
    api project(':services')
}

tasks.named('test') {
    useJUnitPlatform()
}

eclipse {
    classpath {
        baseSourceOutputDir = file('build/eclipse')
    }
}